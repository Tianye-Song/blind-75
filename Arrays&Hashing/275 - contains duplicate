# Python 3
# First version
class Solution:
    def containsDuplicate(self, nums: List[int]) -> bool:
        nums_set = set(nums)
        if len(nums_set)!=len(nums):
            return True
        else:
            return False

# Second version
class Solution:
    def containsDuplicate(self, nums: List[int]) -> bool:
        nums_set = set()
        for x in nums:
            if x in nums_set:
                return True
            nums_set.add(x)
        return False
